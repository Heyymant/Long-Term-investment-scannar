[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    slow: longer-running tests (engine consistency, full walk-forward)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
